# Installation of helmfile 

```
# as root
cd /usr/local/bin 
curl -L https://github.com/helmfile/helmfile/releases/download/v1.1.6/helmfile_1.1.6_linux_386.tar.gz -o helmfile.tar.gz
tar xvfz helmfile.tar.gz 
rm -f README* 
rm -f LICENSE 
rm -f helmfile.tar.gz
```
